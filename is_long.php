<?php   
    // variável do tipo String
    $nome = 'Meu nome Wilbert';
    echo "A variável \$nome é inteira longa? " . (is_int($nome) ? 'Sim' : 'Não') . "<br>";

    // variável do tipo inteiro
    $ano = 2023;
    echo "A variável \$ano é inteira longa? " . (is_int($ano) ? 'Sim' : 'Não') . "<br>";

    // variável do tipo float 
    $pi = 3.14159265; // Troque a vírgula por ponto
    echo "A variável \$pi é inteira
